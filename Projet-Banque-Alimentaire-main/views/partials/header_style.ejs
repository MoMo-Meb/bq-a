<style>
    header {
        width: 100%;
    }
    
    body {
        display: flex;
        flex-direction: column;
        min-height: 100vh;
        margin: 0;
    }

    .responsive-image {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        object-fit: contain;
    }

    nav {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 30px 50px;
        background: transparent;
        border-radius: 0 0 3px 3px; /* Add this line */
    }

    .nav-background {
        background-image: linear-gradient(to bottom, rgb(0, 0, 0), rgb(0, 0, 0));
        border-radius: 0 0 3px 3px; /* Add this line */
    }

    .nav-menu {
        display: flex;
        list-style-type: none;
        justify-content: center;
        flex-grow: 1;
    }

    .nav-menu li {
        margin: 0 20px;
    }

    .nav-background {
        background-image: linear-gradient(to bottom, rgb(0, 0, 0), rgb(0, 0, 0));
    }

    .nav-menu a {
        text-decoration: none;
        color: #ffffff;
        font-style: times;
        font-size: 20px;
        position: relative;
    }

    .nav-menu a:hover {
        color: #ffbf00;
    }

    .nav-menu a::after {
        content: "";
        position: absolute;
        bottom: -5px;
        left: 0;
        width: 100%;
        height: 2px;
        background-color: #838383;
        transform: scaleX(0);
        transition: transform 0.3s;
    }

    .nav-menu a:hover::after {
        transform: scaleX(1);
    }

    .login-logo {
        text-decoration: none;
        color: #FFFFFF;
        font-size: 14px;
        font-weight: bold;
        padding: 10px 15px;
        border-radius: 4px;
        position: relative;
        cursor: pointer;
        width: 64px;
        height: 64px;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .login-menu {
        display: none;
        position: absolute;
        top: 100%;
        right: 0;
        background-color: #ffffff;
        border-radius: 4px;
        box-shadow: 0 4px 6px rgba(255, 255, 255, 0.1);
        z-index: 1;
    }

    .login-menu a {
        display: block;
        padding: 10px 20px;
        text-decoration: none;
        color: #ffffff;
        color: #575757;
        font-size: 16px;
    }

    .login-menu a:hover {
        background-color: #7e7e7e;
        color: #424242;
    }

    .login-logo:hover .login-menu {
        display: block;
    }
</style>